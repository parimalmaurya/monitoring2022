covid
library (spatstat)
attach(covid) 

covid_planar <- ppp(x=lon, y=LAT, c(-180,180), c(-90,90)
plot (covid-planar)
density_map <- density (covid_planar)
plot(density_map)
points (covid_plannar)
cl <- colorRampalette (c("yellow', 'ofrange' "red')) (100)
colorRampalette (('cyan''coral' 'chartreuse')) (3)
plot (density _map, col=cl
points (covid planar, pch=17, col="blue")
Exercise- Change of colours
cl <- colorRampPalette (c( <yellow
colorRampPallete(c("bisque" , "azur". "brown")
plot(density_map, col=cl)
install.packages (spatstat)
library (spatstat)
intall.packages (rgdal)
librabry (rgdal)
setwd ("C:~/lab/)
# lets add coast lines
c oastlines <- readOGR("ne_10m_coastline.shp")
#bringing the map we used yeatetrday
covid <- read.table ("covid_agg.csv", header=t)
covid_planar <- ppp(x= lon, y= lat, c (-180, 180), c (-90, 90)
covid_plane <- ppp(x=covid$lon, Y=covid$lat, c(-180,180), c(-90,90))
>=plot (covid-planar)
>plot ( coastlines)
density_map <- density(covid_planar)
plot (density_map)
> points (covid_planar, pch=18, col="green")
plot (coastlines, add=T)
plot (coastlines, add=T, col="yellow")
head()
# using interpolation
> marks (my pattern 
> attach (covid)
> marks (covid_planar) <- cases
cases_map <- Smooth (covid_planar)
plot (coastlines
